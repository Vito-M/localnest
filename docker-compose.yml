version: '3'
services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./app/data:/app/data # per i file upload
      - ./app/data/database.db:/app/data/database.db  # per il DB SQLite persistente
